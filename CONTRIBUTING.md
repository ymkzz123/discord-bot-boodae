# 기여 가이드

1. 새 브랜치를 만듭니다: `feat/<짧은-이름>` 또는 `fix/<짧은-이름>`
2. 작은 단위로 변경하고 순수 로직에는 테스트를 추가합니다.
3. `npm run check && npm run build`를 실행합니다.
4. 토큰, `.env`, 사용자 질문 원문이 커밋이나 로그에 없는지 확인합니다.
5. PR 템플릿을 채우고 동작 변화와 테스트 방법을 설명합니다.

명령 처리기에 외부 SDK 호출을 직접 추가하지 말고 서비스 모듈을 만든 뒤 주입하는 구조를 유지하세요.
